export class OrgService {
    public async getAuthorizedOrg(): Promise<string> {
        // Placeholder until Salesforce CLI integration is added.
        return Promise.resolve('No org connected yet');
    }
}
